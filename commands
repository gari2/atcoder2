for atcoder
<submit（atam）>
tam (filename)(space)(type of contest)(space)(contest number)
? 言語を選んでね！！！！ >>  C++14 (GCC 5.4.1)
? 問題を選んでね！！！！ >>  A~D choose number

<dl sample>
oj URL
<test>
g++ (filename)
oj test

<debug>
g++ -g (filename)

・workflow
<make directory>
acc new (name of contest)
<debug>
<test>
g++ (filename)
oj test
<submit()>
tam (filename)(space)(type of contest)(space)(contest number)
? 言語を選んでね！！！！ >>  C++14 (GCC 5.4.1)
? 問題を選んでね！！！！ >>  A~D choose number
<for next problem....>
acc add 
.....

rm -rf test

atcoder-cliを使ったら、もっと便利になる可能性ありそう
具体的には、　問題ごとにディレクトリを分けることでrmの必要が無くなりそう

<atcoder-cli>
acc new (contestname)
cd (contestname)
acc add -> choose problem(A~D) 
acc s(AOJとAtcoderでID名が違っているので私の場合できなそう、submitにはatamを使う)

<テンプレートの設定（atcoder-cli）>
cd `acc config-dir`
cd cpp
vim cpp -> 変更後、:wq
<githubとの連携>
git commit -am "add feature"
git add -A
git push
